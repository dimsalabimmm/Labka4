<UserControl x:Class="Labka4.Controls.Surface3DControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="400"
             d:DesignHeight="300">
    <Grid x:Name="RootGrid" Background="{StaticResource Brush.CardBackground}">
        <Border CornerRadius="18"
                BorderBrush="{StaticResource Brush.PanelBorder}"
                BorderThickness="1"
                Background="{StaticResource Brush.PanelBackground}"
                Padding="10"
                Effect="{StaticResource DropShadowEffect}">
            <Viewport3D x:Name="MainViewport"
                        ClipToBounds="True">
                <Viewport3D.Camera>
                    <PerspectiveCamera x:Name="MainCamera"
                                        Position="0,0,8"
                                        LookDirection="0,0,-8"
                                        UpDirection="0,1,0"
                                        FieldOfView="60" />
                </Viewport3D.Camera>
                <ModelVisual3D>
                    <ModelVisual3D.Content>
                        <AmbientLight Color="#404040" />
                    </ModelVisual3D.Content>
                </ModelVisual3D>
                <ModelVisual3D>
                    <ModelVisual3D.Content>
                        <DirectionalLight Color="#FFFFFFFF" Direction="-1,-1,-3" />
                    </ModelVisual3D.Content>
                </ModelVisual3D>
                <ModelVisual3D>
                    <ModelVisual3D.Content>
                        <DirectionalLight Color="#FFBFBFBF" Direction="1,1,-2" />
                    </ModelVisual3D.Content>
                </ModelVisual3D>
                <ModelVisual3D x:Name="SurfaceVisual" />
            </Viewport3D>
        </Border>
        <Border HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="16"
                Padding="10,6"
                Background="#AA05070F"
                BorderBrush="{StaticResource Brush.AccentPrimary}"
                BorderThickness="1"
                CornerRadius="8">
            <StackPanel>
                <TextBlock Text="Орбита · удерживайте ПКМ"
                           Foreground="{StaticResource Brush.TextPrimary}"
                           FontSize="12"
                           FontWeight="SemiBold" />
                <TextBlock Text="Зум · колесо мыши"
                           Foreground="{StaticResource Brush.TextSecondary}"
                           FontSize="11" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

